<section>
  <div id="clearance">
    <h2><%= t(".title") %></h2>

      <%= render partial: '/users/form' %>

      <div class="other-links">
        <%= link_to t("Déjà un compte ?"), sign_in_path %>
      </div>
  </div>
</section>